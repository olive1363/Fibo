# FiboSwanny market history

`BTC_4h.json` and `ETH_4h.json` are generated automatically by the GitHub Actions workflow **Update crypto history**.

The HTML loads these same-origin files before trying external market APIs.
